apiVersion: apps/v1
kind: Deployment
metadata:
  name: "${{ values.repoName }}"
  labels:
    app: "${{ values.repoName }}"
  annotations:
    argocd.argoproj.io/hook: PreSync
    argocd.argoproj.io/hook-delete-policy: HookSucceeded
spec:
  replicas: 1
  selector:
    matchLabels:
      app: "${{ values.repoName }}"
  template:
    metadata:
      labels:
        app: "${{ values.repoName }}"
    spec:
      securityContext:
        fsGroup: 65534
      containers:
        # 1. Web Server Engine Application Core Runtime Layer
        - name: app-container
          image: "${{ values.image }}"
          ports:
            - containerPort: ${{ values.port }}
          env:
            - name: "${{ values.serviceType | upper }}_PORT"
              value: "${{ values.port }}"
          volumeMounts:
            - name: code-stream-volume
              mountPath: "${{ values.webRootPath }}"
            - name: config-volume
              mountPath: "/usr/local/apache2/conf/httpd.conf"
              subPath: "httpd.conf"
          resources:
            requests:
              cpu: "${{ values.cpuRequest }}"
              memory: "${{ values.memRequest }}"
            limits:
              cpu: "${{ values.cpuLimit }}"
              memory: "${{ values.memLimit }}"

        # 2. Automated Git-Sync Synchronization Engine Layer
        - name: git-sync-sidecar
          image: "registry.k8s.io/git-sync/git-sync:v4.7.1"
          args:
            - "--repo=https://github.com/${{ values.repoOwner }}/${{ values.repoName }}.git"
            - "--branch=main"
            - "--depth=1"
            - "--wait=10"
            - "--root=/tmp/git"
            - "--dest=${{ values.repoName }}"
          volumeMounts:
            - name: code-stream-volume
              mountPath: /tmp/git
          securityContext:
            runAsUser: 65534
            runAsGroup: 65534
          resources:
            requests:
              cpu: "${{ values.cpuRequest }}"
              memory: "${{ values.memRequest }}"
            limits:
              cpu: "${{ values.cpuLimit }}"
              memory: "${{ values.memLimit }}"

      volumes:
        - name: code-stream-volume
          emptyDir: {}
        - name: config-volume
          configMap:
            name: "${{ values.repoName }}-httpd-config"
---
apiVersion: v1
kind: ConfigMap
metadata:
  name: "${{ values.repoName }}-httpd-config"
data:
  httpd.conf: |
    ServerRoot "/usr/local/apache2"
    Listen ${{ values.port }}

    # 1. Load the core modules first
    LoadModule mpm_event_module modules/mod_mpm_event.so
    LoadModule authn_core_module modules/mod_authn_core.so
    LoadModule authz_core_module modules/mod_authz_core.so
    LoadModule dir_module modules/mod_dir.so
    LoadModule mime_module modules/mod_mime.so
    LoadModule log_config_module modules/mod_log_config.so
    LoadModule env_module modules/mod_env.so
    LoadModule headers_module modules/mod_headers.so
    LoadModule rewrite_module modules/mod_rewrite.so
    LoadModule unixd_module modules/mod_unixd.so

    <IfModule unixd_module>
        User daemon
        Group daemon
    </IfModule>

    TypesConfig conf/mime.types
    CustomLog /proc/self/fd/1 combined
    ErrorLog /proc/self/fd/2
    LogLevel warn

    ServerTokens Prod
    ServerSignature Off

    DocumentRoot "${{ values.webRootPath }}/${{ values.repoName }}"

    <Directory "${{ values.webRootPath }}/${{ values.repoName }}">
        Options -Indexes +FollowSymLinks
        AllowOverride None
        Require all granted

        # SPA Engine Forwarding Logic
        RewriteEngine On
        RewriteBase /
        RewriteRule ^index\.html$ - [L]
        RewriteCond %{REQUEST_FILENAME} !-f
        RewriteCond %{REQUEST_FILENAME} !-d
        RewriteRule . /index.html [L]
    </Directory>