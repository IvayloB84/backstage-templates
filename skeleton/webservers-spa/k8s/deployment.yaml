apiVersion: apps/v1
kind: Deployment
metadata:
  name: "${{ values.repoName }}"
  labels:
    app: "${{ values.repoName }}"
spec:
  replicas: 1
  selector:
    matchLabels:
      app: "${{ values.repoName }}"
  template:
    metadata:
      labels:
        app: "${{ values.repoName }}"
    spec:
      securityContext:
        fsGroup: 65534
      containers:
        # 1. Web Server Engine Application Core Runtime Layer
        - name: app-container
          image: "${{ values.image }}"
          command: ["/bin/sh", "-c"]
          args:
            - |
              echo "Waiting for git-sync sidecar to populate directory..."
              while [ ! -d "${{ values.webRootPath }}/${{ values.repoName }}" ]; do
                sleep 2
              done
              echo "Directory found! Launching asset server."
              nginx -g 'daemon off;'
          ports:
            - containerPort: ${{ values.port }}
          env:
            - name: "${{ values.serviceType | upper }}_PORT"
              value: "${{ values.port }}"
          volumeMounts:
            - name: code-stream-volume
              mountPath: "${{ values.webRootPath }}"
            - name: config-volume
              mountPath: "/etc/nginx/conf.d/default.conf"
              subPath: "default.conf"
          resources:
            requests:
              cpu: "${{ values.cpuRequest }}"
              memory: "${{ values.memRequest }}"
            limits:
              cpu: "${{ values.cpuLimit }}"
              memory: "${{ values.memLimit }}"

        # 2. Automated Git-Sync Synchronization Engine Layer
        - name: git-sync-sidecar
          image: "registry.k8s.io/git-sync/git-sync:v4.7.1"
          args:
            - "--repo=https://github.com/${{ values.repoOwner }}/${{ values.repoName }}.git"
            - "--ref=main"
            - "--depth=1"
            - "--period=10s"
            - "--root=/tmp/git"
            - "--link=${{ values.repoName }}"
          volumeMounts:
            - name: code-stream-volume
              mountPath: /tmp/git
          securityContext:
            runAsUser: 65534
            runAsGroup: 65534
          resources:
            requests:
              cpu: "${{ values.cpuRequest }}"
              memory: "${{ values.memRequest }}"
            limits:
              cpu: "${{ values.cpuLimit }}"
              memory: "${{ values.memLimit }}"

      volumes:
        - name: code-stream-volume
          emptyDir: {}
        - name: config-volume
          configMap:
            name: "${{ values.repoName }}-nginx-config"
---
apiVersion: v1
kind: ConfigMap
metadata:
  name: "${{ values.repoName }}-nginx-config"
data:
  default.conf: |
    server {
        listen       ${{ values.port }};
        listen  [::]:${{ values.port }};
        server_name  localhost;

        location / {
            root ${{ values.webRootPath }}/${{ values.repoName }};
            autoindex on;
            try_files \$uri \$uri/ @spa;
        }

        location @spa {
            root ${{ values.webRootPath }}/${{ values.repoName }};
            try_files /index.html =404;
        }
    }