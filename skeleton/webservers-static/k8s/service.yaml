apiVersion: v1
kind: Service
metadata:
  name: "${{ values.repoName }}-static-svc"
  labels:
    app: "${{ values.repoName }}-static"
spec:
  type: ClusterIP
  ports:
    - port: 80
      targetPort: ${{ values.port }}
      protocol: TCP
      name: http
  selector:
    app: "${{ values.repoName }}-static"
