apiVersion: backstage.io/v1alpha1
kind: Location
metadata:
  name: remote-centralized-templates
  description: Index of all pipeline scaffolding template cards
spec:
  targets:
    - ./manifest/github-repo-template.yaml
    - ./manifest/create-guarded-component.yaml
    - ./manifest/deactivate-guarded-component.yaml