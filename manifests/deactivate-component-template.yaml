apiVersion: scaffolder.backstage.io/v1beta3
kind: Template
metadata:
  name: deactivate-component-template
  title: Deactivate Cluster Component
  description: Safely tears down active pipeline structures and cleans GitOps anchors
spec:
  owner: user:default/IvayloB84
  type: service


  parameters:
    - title: Identify Target Component to Deactivate
      required:
        - repoName
        - repoOwner
      properties:
        repoName:
          title: Component Repository Name
          type: string
          description: Enter the exact lowercase name of the application folder to remove.
        repoOwner:
          title: Repository Owner
          type: string
          default: IvayloB84
          description: Your GitHub organization or personal profile username
        isAutomated:
          type: boolean
          default: false
          title: Automated Trigger
          description: Internal flag used to toggle read-only states

      # UI Schema conditions control visibility and interactivity
      uiSchema:
        # Hide the system boolean flag so users don't see it on the form
        isAutomated:
          ui:widget: hidden

        # Apply conditional rules dynamically to repoName based on the state of isAutomated
        repoName:
          ui:options:
            dependencies:
              isAutomated:
                oneOf:
                  - properties:
                      isAutomated:
                        enum: [true]
                    uiSchema:
                      # If isAutomated is true (clicked via button), disable typing
                      ui:disabled: true
                  - properties:
                      isAutomated:
                        enum: [false]
                    uiSchema:
                      # If false (direct navigation), keep it fully active
                      ui:disabled: false

    # ⚙️ PAGE 2: Corporate Safety Gate
    - title: Corporate Safety Gate
      required:
        - confirmDestruction
      properties:
        confirmDestruction:
          title: "Infrastructure Destruction Authorization"
          type: boolean
          default: false
          enum:
            - true
      uiSchema:
        confirmDestruction:
          ui:widget: "checkbox"
          ui:description: |
            <span style="color: #d32f2f; font-weight: bold; font-size: 1.1em;">
               WARNING: I explicitly authorize the total destruction of this component repository and all its infrastructure pipelines. Checking this box is mandatory to proceed.
            </span>

  steps:
    - id: generate-uuid
      name: Generating Unique Session Token
      action: debug:log
      input:
        message: "Deactivation session identifier instantiated for repo: ${{ parameters.repoName }}"

    - id: create-pr
      name: Generate GitOps Deletion Pull Request
      action: publish:github:pull-request
      input:
        repoUrl: 'github.com?repo=ArgoCD-RootApps&owner=${{ parameters.repoOwner }}'
        token: ${{ secrets.GITHUB_TOKEN }}
        branchName: purge/${{ parameters.repoName }}
        title: "fix(gitops): purge infrastructure files for ${{ parameters.repoName }}"
        filesToDelete:
          - 'argocd-apps/${{ parameters.repoName }}/argocd-app.yaml'
          - 'argocd-apps/${{ parameters.repoName }}/k8s/deployment.yaml'
          - 'argocd-apps/${{ parameters.repoName }}/k8s/service.yaml'
          - 'argocd-apps/${{ parameters.repoName }}/catalog-info.yaml'
          - 'argocd-apps/${{ parameters.repoName }}/config.yaml'
        description: |
          ### Automated Component Deletion Request
          This PR cleanly removes the application and service manifests inside `./argocd-apps/${{ parameters.repoName }}/k8s/`.

          Expected Outcomes upon merging:
          * Argo CD auto-pruning triggers immediately.
          * Target pods, configurations, and folders are removed from the cluster.
        targetBranchName: main


  output:
    links:
      - title: Review Deletion Pull Request
        url: ${{ steps['create-pr'].output.remoteUrl }}
